
import 'package:flutter/material.dart';

class SalaAdmin extends StatelessWidget {
  const SalaAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('bem vindo user');
  }
}